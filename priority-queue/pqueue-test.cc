/* A priority queue client */

#include <iostream>
#include "max-pqueue.h"
#include <string> /* stoi */

int main(int argc, char *argv[]) {
	/* Print the top M lines in the input stream */
	int M = std::stoi(std::string(argv[0]));
	return 0;
}
