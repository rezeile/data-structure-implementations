// stack.h: header file
/* class definition */
template< class T>
class Stack {
   T items[MaxStack];
   int top;
public:
   Stack();
   // ~Stack();
   // void push(Typ);
   // Typ pop();
   // int empty();
   // int full();
};

/* class definiton */
template < class T> 
Stack<T>::Stack() {
	top = -1;
}